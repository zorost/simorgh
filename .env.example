# Optional. Only for local chat tests. Never commit a real key.
# The GitHub Pages app stores the visitor's key in localStorage only.
OPENROUTER_API_KEY=
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
OPENROUTER_DEFAULT_MODEL=openai/gpt-4.1-mini
