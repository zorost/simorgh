# Security

- No API keys, tokens, or account data belong in this repository.
- Chat keys stay in the visitor's `localStorage`. They are never posted.
- `.env` is gitignored. Use `.env.example` as the shape.
- The site fetches public ganjoor-data JSON. It does not scrape authenticated Ganjoor pages.
